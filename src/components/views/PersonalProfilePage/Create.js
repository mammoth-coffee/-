/*import React from 'react'
import style from './PersonalProfilePage.module.css'

function Create(props) {
  return (
    
    
    <form onSubmit={event=>{
      event.preventDefault();
      const title = event.target.title.value;
      const body = event.target.body.value;
      props.onCreate(title, body);
    }}>
      <p><input type="text" name="title" placeholder="title" className={style.name}/></p>
      <p><textarea name="body" placeholder="body" className={style.email}></textarea></p>
      <p><input type="submit" value="Create" className={style.btn}></input></p>
    </form>
  
  )
}

export default Create*/